-- Internetwork --

Internetwork o Internet: un insieme di reti diverse collegate tramite un gateway. Ogni rete utilizza hardware diverso e, cosa ancora più importante, protocolli diversi per gestire la comunicazione interna.

-- Protocolli --

La comunicazione tra le varie entità all'interno delle reti è gestita da protocolli. 
Un protocollo di rete è un insieme formale e predefinito di regole che regolano le interazioni tra due o più dispositivi elettronici collegati per facilitare la comunicazione. 
Queste regole riguardano principalmente i componenti software dei dispositivi. Le prime reti di computer erano incentrate principalmente sull'hardware, considerando il software solo come un complemento.
Questo approccio è ormai superato. Nei contesti moderni, il software di rete è strutturato in modo complesso e svolge un ruolo centrale nella funzionalità della rete.

-- Ruolo centrale del Software -- 

Complessità delle funzionalità:
Le reti moderne devono supportare un'ampia gamma di servizi e applicazioni, dalla semplice trasmissione di dati alle complesse operazioni di sicurezza, alla gestione del traffico, all'ottimizzazione delle prestazioni e all'integrazione cloud. 
Queste funzioni richiedono un software sofisticato in grado di gestire dinamicamente varie esigenze e configurazioni.

Adattabilità e scalabilità:
Le reti odierne devono essere altamente adattabili e scalabili per rispondere rapidamente alle mutevoli esigenze delle aziende o dei consumatori.
Il software di rete consente questa flessibilità, permettendo di implementare aggiornamenti e modifiche in modo molto più rapido e a costi inferiori rispetto alle modifiche hardware.

Sicurezza:
La sicurezza delle reti moderne dipende in larga misura da software avanzati che implementano firewall, sistemi di rilevamento delle intrusioni, crittografia e altre misure di sicurezza essenziali per proteggere i dati e mantenere la rete al sicuro da
minacce sempre più sofisticate.

Il software di rete è fondamentale non solo per gestire le funzionalità, ma anche per garantire la compatibilità e la comunicazione tra diversi componenti hardware.
Agendo da intermediario, consente a dispositivi diversi, potenzialmente di vari produttori, di funzionare insieme senza soluzione di continuità.
Questa interoperabilità è fondamentale per costruire sistemi di rete flessibili ed efficienti in grado di adattarsi alle nuove tecnologie e alle nuove esigenze.

-- Pila di strati --

Per semplificare la progettazione, le reti sono strutturate in livelli che sono costruiti uno sopra l'altro.

Le differenze tra le reti possono includere:
    - Il numero di livelli
    - I nomi dei livelli
    - Il contenuto di ciascun livello
    - La funzionalità di ciascun livello

Ciascun livello serve a fornire servizi specifici ai livelli sopra di esso, mentre astrae i dettagli di come tali servizi sono implementati.

-- Layer Protocol --

Quando lo stesso livello di rete, denominato livello n, comunica tra macchine diverse, le regole e le convenzioni che regolano tale comunicazione sono note collettivamente come protocollo di livello n.
Un protocollo è essenzialmente un accordo che stabilisce le procedure di comunicazione tra le parti coinvolte.

La comunicazione tra il livello n-esimo di una macchina e il livello n-esimo di un'altramacchina è resa possibile dai servizi offerti dal livello sottostante (n-1).

-- Comunicazione di Rete: Analogia --

Punti di inizio e fine:
Il processo inizia nel punto A, dove una persona (il mittente) esprime un messaggio: “Mi piacciono i conigli”. 
Termina nel punto B, dove un filosofo (il destinatario) riceve il messaggio tradotto in francese: "J'aime bien les lapins". 
Nel networking, questi punti rappresentano i nodi di origine e destinazione.

Livelli di trasmissione e traduzione:
Il messaggio attraversa diverse fasi prima di raggiungere il filosofo. 
Inizialmente, viene tradotto in olandese e passa attraverso vari intermediari (segretaria e traduttore) che gestiscono e inoltrano le informazioni, analogamente ai dati che passano attraverso più dispositivi di rete come router e switch.

Analogia con i pacchetti di dati:
Il messaggio originale “Mi piacciono i conigli” può essere visto come pacchetti di dati. 
Mentre si sposta dalla fonte, viene elaborato e riconfezionato in varie fasi: la traduzione in un'altra lingua può rappresentare la codifica o la crittografia in termini di rete.

--------------------------------------------------------

Routing:
L'uso dei fax e di personale diverso per l'elaborazione delle informazioni è simile al modo in cui i pacchetti di dati vengono instradati attraverso diversi percorsi di rete. 
Ogni nodo (persona o dispositivo) decide come e dove inviare i dati in base ai protocolli di rete.

Livelli di protocollo:
Ogni fase del diagramma in cui il messaggio viene gestito ed elaborato rappresenta diversi livelli nei protocolli di rete (come il modello OSI). 
Ad esempio, la segretaria potrebbe essere vista come operante nel livello di presentazione, traducendo i formati dei dati, mentre il traduttore opera nel livello applicativo, convertendo il contenuto del messaggio in diverse lingue.

Consegna end-to-end:
Proprio come il messaggio inizia come una semplice espressione e finisce per essere compreso da qualcuno che parla una lingua diversa,
nel networking i dati partono da una fonte e vengono consegnati a una destinazione attraverso vari segmenti di rete e possibilmente trasformati o protetti nel processo, garantendo che siano utilizzabili al momento della ricezione.

-- Comunicazione di Rete --

I dati non vengono trasferiti direttamente dal livello n di una macchina al livello n di un'altra macchina.

- Origine (top-down): Ogni livello trasmette i dati e le informazioni di controllo al livello immediatamente inferiore, continuando fino a raggiungere il livello più basso.

- Destinazione (bottom-up): ogni livello passa i dati e le informazioni di controllo al livello immediatamente
superiore, continuando fino a raggiungere il livello più alto.

Interfaccia: specifica le operazioni e i servizi che il livello inferiore fornisce a quello superiore.

Al di sotto del livello 1, il mezzo fisico facilita il trasferimento dei dati dall'host 1 all'host 2.

-- Comunicazione di Rete: Esempio tecnico --

Il programma applicativo deve inviare il messaggio M alla sua entità peer (ovvero lo stesso programma applicativo su un altro computer).

1. Il livello 5 consegna il messaggio M al livello 4.

2. Il livello 4 aggiunge un'header 𝑯𝟒 al messaggio. 
L'header contengono informazioni di controllo quali numero di sequenza, offset dei dati, numero di conferma, dimensione, ecc.
Il livello 4 consegna il messaggio M al livello 3.

3. Il livello 3 divide i dati in unità più piccole (pacchetti) e aggiunge un'header 𝑯𝟑 a ciascun pacchetto.
Le intestazioni contengono gli indirizzi IP di origine e destinazione, la lunghezza totale, un identificatore univoco per il pacchetto, la lunghezza dell'intestazione, il TTL ecc. 
Il livello 3 consegna i pacchetti al livello 2.

4. Il livello 2 aggiunge un'intestazione 𝑯𝟐 e un trailer 𝑻𝟐 a ogni pacchetto ricevuto denominato frame.
Le intestazioni contengono gli indirizzi MAC di origine e destinazione,la lunghezza del payload, ecc. 
Il trailer viene utilizzato per il controllo degli errori di trasmissione. 
Il livello 2 consegna i pacchetti al livello 1.

5. Il livello 1, il livello fisico, trasferisce i dati alla macchina di destinazione.

6. Nella macchina di destinazione, il messaggio si sposta verso l'alto attraverso ogni livello, con le intestazioni e le code che vengono rimosse ad ogni passaggio. 
Il messaggio viene completamente ricostruito e diventa leggibile una volta raggiunto il livello 5.

-- Servizi Connection-oriented --

I servizi orientati alla connessione sono quelli in cui viene stabilita una connessione dedicata tra i due nodi di comunicazione prima dell'inizio del trasferimento dei dati. 
Questo tipo di servizio garantisce che i dati vengano trasmessi in modo affidabile e nell'ordine corretto.

Caratteristiche principali dei servizi orientati alla connessione:

    - Affidabilità: La trasmissione dei dati è garantita e gli errori di trasmissione vengono rilevati e corretti. 
                    Utilizzati in applicazioni in cui l'affidabilità è più importante della velocità, come la posta elettronica e l'online banking.
                    
    - Ordinamento: I dati arrivano nell'ordine in cui sono stati inviati.
    
    - Controllo del flusso: Regola la quantità di dati inviati per evitare di sovraccaricare il destinatario.
    
    - Stabilire e chiudere la connessione: Richiede un processo iniziale di handshake per stabilire la connessione e un processo di chiusura per terminarla.

Handshaking (Apertura della connessione)

1. Richiesta di connessione: Il dispositivo A invia un messaggio di richiesta al dispositivo B per avviare la connessione.

2. Risposta: Il dispositivo B risponde al dispositivo A, confermando di aver ricevuto la richiesta e di essere pronto per comunicare.

3. Conferma: Il dispositivo A invia una conferma di aver ricevuto la risposta del dispositivo B.

Chiusura della connessione

1. Richiesta di terminazione: Una volta completata la comunicazione, il dispositivo A invia una richiesta di terminazione al dispositivo B.

2. Conferma: Il dispositivo B conferma la richiesta di terminazione, indicando che è pronto a chiudere la connessione.

3. Conferma finale: Il dispositivo A invia una conferma finale, completando il processo di terminazione.

Il servizio ACK (Acknowledgment) è un meccanismo utilizzato per garantire che i dati inviati siano correttamente ricevuti dal destinatario.

1. Invio del pacchetto: Il dispositivo A invia un pacchetto di dati al dispositivo B.

2. Ricezione e conferma: Quando il dispositivo B riceve il pacchetto, invia un messaggio di conferma (ACK) al dispositivo A per indicare che il pacchetto è stato ricevuto correttamente.

    - Ritrasmissione in caso di errore: se il dispositivo A non riceve l'ACK entro un certo periodo di tempo, ritrasmette il pacchetto, supponendo che sia andato perso o danneggiato durante la trasmissione.
      Questo processo continua per ogni pacchetto di dati inviato, garantendo che tutti i pacchetti siano ricevuti correttamente e nel giusto ordine.

-- Servizi Connectionless --

I servizi Connectionless non richiedono la creazione di una connessione dedicata prima dell'invio dei dati.
Ogni pacchetto di dati viene invece trattato in modo indipendente e contiene tutte le informazioni necessarie per raggiungere la sua destinazione.
Due pacchetti con la stessa macchina di origine e destinazione possono viaggiare attraverso percorsi diversi e arrivare in momenti diversi.

Caratteristiche principali dei servizi senza connessione:

    - Semplicità e velocità: Non richiedono un processo di handshake, quindi hanno una latenza inferiore rispetto ai servizi orientati alla connessione.
    
    - Nessuna garanzia di consegna: I pacchetti possono andare persi, essere duplicati o arrivare in ordine errato.
    
    - Indipendenza dei pacchetti: Ogni pacchetto viene trattato separatamente e contiene tutte le informazioni necessarie per raggiungere la sua destinazione.
    
    - Utilizzo in applicazioni specifiche: Utilizzati in applicazioni in cui la velocità è più importante dell'affidabilità, come lo streaming e i giochi online.